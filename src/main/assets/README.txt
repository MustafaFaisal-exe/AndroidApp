# Place yolo11n.onnx here. Export from Python with:
# from ultralytics import YOLO
# YOLO('yolo11n.pt').export(format='onnx', imgsz=320, simplify=True)
# Then copy yolo11n.onnx into this folder and rebuild.
